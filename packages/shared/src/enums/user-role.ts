export enum UserRole {
  USER_FREE = 'user_free',
  USER_PRO = 'user_pro',
  RESELLER = 'reseller',
  SUPPORT_AGENT = 'support_agent',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super_admin',
}

export enum ProfileStatus {
  ACTIVE = 'active',
  BANNED = 'banned',
  DELETING = 'deleting',
  DELETED = 'deleted',
}
